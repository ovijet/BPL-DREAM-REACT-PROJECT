import React from "react";

const SelectedPlayer = ({ selecetedPlayer }) => {
  console.log(selecetedPlayer, "pppppppppp");
  return (


    <h1>ovi</h1>
    // <div>
    //   <h2 className="text-xl font-bold">Selected: {selecetedPlayer.length}</h2>

    //   {selecetedPlayer.map((player, index) => (
    //     <div key={index} className="card bg-base-100 shadow p-4 mb-3">
    //       <h3 className="font-bold">{player.playerName}</h3>
    //       <p>{player.playerCountry}</p>
    //       <p>{player.playerType}</p>
    //       <p>{player.price}</p>
    //     </div>
    //   ))}
    // </div>
  );
};

export default SelectedPlayer;
